{
    "name": "@icapps/nestjs",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*",
        "shared/*",
        "example"
    ],
    "scripts": {
        "build": "turbo run build",
        "test": "turbo run test",
        "lint": "turbo run lint",
        "format": "prettier --ignore-path .gitignore --cache --write ."
    },
    "devDependencies": {
        "prettier": "^2.8.4",
        "turbo": "^1.8.1"
    }
}
