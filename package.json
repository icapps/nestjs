{
    "name": "@icapps/nestjs",
    "version": "1.0.0",
    "private": true,
    "workspaces": [
        "packages/*",
        "shared/*"
    ],
    "scripts": {
        "build": "turbo run build",
        "test": "turbo run test",
        "lint": "turbo run lint",
        "format": "prettier --ignore-path .gitignore --cache --write ."
    },
    "devDependencies": {
        "prettier": "latest",
        "turbo": "latest"
    },
    "engines": {
        "node": ">=14.0.0"
    },
    "packageManager": "npm@8.19.2"
}
